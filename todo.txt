### images problem
also sequrity origin problem in chrome (now I run a web server)
    and images problem was exposed. images keep loading over and over again 
    from the web server. maybe I need an accurate image preload mechanism
    with finish load callback, or image.onLoad or somthn

    maybe set isLoaded flag to sprite object
    and wait for arr resources loaded



I need an extra hidden canvas to work with opacity, image processing - rotation, etc

Images preloader.

plant.isCollision(obj, obj)
plant.isCollision(ArrayOfObj, obj)
plant.isCollision(obj, ArrayOfObj)
plant.isCollision(ArrayOfObj, ArrayOfObj)



///////////// BERRY ////////////////////////
var gameloop = new Plant.Gameloop({
    interval: "20",
});


// extend myPlayer with
Berry(myPlayer).jumpy({
  gravity: 1,
  acceleration: 100,
  power: 70
});

myPlayer.jump();
myPlayer.isJumping (true|false)

myPlayer.fadeOut();


// keybinder?

var myKeybinder = new Berry.Keybinder();
myKeybinder['A'].keyDown(function(){
  myPlayer.jump();
});

OR

myKeybinder['Space'].keyDown = function(){
  myPlayer.jump();
});



// extend whole Plant obj with Berry? (Extend constructors inside Plant)
// or extend single instance object?
 
