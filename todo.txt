### opacity
add opacity property to all primitives

### click on primitives or sprite
add options for all layers (z-indexes)

### sprite effects for game loop
fadeOut(frames, callback);
fadeIn?
turn(angle, frames, callback) or -angle
moveTo(x, y, frames, callback)
moveLeft moveRight moveUp moveBottom

also, add angle property to all objects

### improve collision check
plant.isCollision(obj, obj)
plant.isCollision(ArrayOfObj, obj) ??
plant.isCollision(obj, ArrayOfObj)
plant.isCollision(ArrayOfObj, ArrayOfObj)

### research
plant.isCollision(one, two, once:true|false)
add boolean option to isCollision, fire colision event once or continue
##########

### keybinder?

var myKeybinder = new plant.Keybinder();
myKeybinder['A'].keyDown(function(){
  myPlayer.jump();
});

OR
myKeybinder['Space'].keyDown = function(){
  myPlayer.jump();
});

///////////// BERRY ////////////////////////

// extend myPlayer with
Berry(myPlayer).jumpy({
  gravity: 1,
  acceleration: 100,
  power: 70
});

myPlayer.jump();
myPlayer.isJumping (true|false)

// extend whole Plant obj with Berry? (Extend constructors inside Plant)
// or extend single instance object?
 
