# CLAUDE.md

## Project

Plant.js — a lightweight 2D canvas game engine written in TypeScript.

## Rules

- Always update the documentation when making changes to the Plant.js library.
- Always run `npm run typecheck` after implementing changes to TypeScript code to verify there are no type errors.
