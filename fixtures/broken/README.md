# Broken fixtures

Deliberately invalid levels, one per structural rule in
`docs/level-generation.md` §5, used to test that `sokoban/validate.ts`
rejects each failure mode. Each file is named after the `StructuralIssue`
code it's expected to trigger.
